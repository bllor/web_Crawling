python 라이브러리
requests : http패킷을 python파일로 요청을 만들어주는 것
selenium : 브라우저의 역할을 대신해주는 라이브러리

request라이브러리는 파라미터는 다 임의로 입력해주어야 하는데,
selenium은 브라우저를 건들여서 사용하는 것이므로, 서버에서는 구분하기 힘들다.

request는 속도가 빠르다는 장점이 있다.
selenium은 메모리 할당이 필요하고 창을 하나 더 띄운다는 단점이 있다. 하지만 브라우저를 사용하는 것이므로
서버에서 거의 막을 수 없다.
